# Immich Deployment

This project is a Open Tofu scripts for deploying Immich into an instance in Catalyst Cloud

Currently it creates a demo server that only allows access from a single source IP.

TODO

- Use caddy for TLS termination
- Use certbot with metaname plugin to get SSL certs